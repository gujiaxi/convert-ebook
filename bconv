#!/usr/bin/env bash

cwd_dir=$(dirname $(realpath $0))

if [ "$#" -lt 1 ]; then
    echo "[ERROR] Illegal number of parameters"
    exit 1
fi

python3 -B ${cwd_dir}/main.py "$@"

echo "[SUCCESS] Ebook is successfully converted."
